待办事宜：

考虑尝试VUE多页面应用；

cookie和session要上，用上vuex的状态管理；
组件之间的数据传递要用上；

后台模拟服务器使用express改写；
在服务器端使用微服务的思路改写和部署；
考虑如何生成类似zlgcloud类似的web接口帮助页面；

调整一下目前引用的菜单的样式；
路由懒加载机制，并实际调试测试，看是否真实地实现了需要时加载；
了解一下路径为啥有#，可否去除？

尝试一下打包发布；
尝试websocket的使用；
考虑在开发的过程中，制定开发规范，比如引入类型、Use strict；
进一步引入测试框架；

了解websocket的使用；



1、基于Vscode搭建好VUE的开发环境；
2、开发一个光伏电站信息概览的Demo，具体要求包括：
（1）具备基本的登录和退出功能，并注意处理好状态和缓存相关；
（2）整体布局要简洁明了，总体采用上下结构，两层标签导航；建议使用elements协助开发；
（3）可以数值的形式，显示获取的总体收益信息，并实时更新；支持多个电站可以切换显示；
（4）可以列表的形式，动态加载个人多个电站的表格信息；
（5）可以基于echarts动态显示日、月、年的收益曲线；
（6）可以考虑自己动手，基于Node.js写一个简易的后台服务器，方便测试；（可选）
（7）引入相关的单元测试；
注：相关模块显示的数据，要求能从后台服务器获取，实时更新，而不是简单的前端模拟。


已初步完成：

组件划分以及目录重构；

组件之间的局部跳转，不通过路由的机制，类似于tab页面；
引入element的元素进来；

尝试着单步调试一下谢的login文件，发现找不到；
谢提交的文件和文件夹的名称，不能重命名；

用路由和v-if机制，分别实现页面切换；
创建一个层级式的组件引用；

尝试一下不使用vue-cli来创建和注册组件；
了解一下主要的组件，电站页面的图表、电站运行状态、个人中心页面的列表、地图组件；
有一个select.vue是干嘛的？
了解一下页面数据的来源；
了解现在的光伏项目的页面切换机制；
vue的网页项目，如何调试？如何监控变量？
vscode的调试控制台，如何使用？有哪些用处？
IDE配置；整体框架代码阅读；核心组件研究；

开通gitlab账号；
安装git相关软件；
git仓库完成关联；
改动IP地址；
申请本机电脑联网权限；
本机调试环境搭建；

做一些基础的网页布局，包括登录页面和主页；
能够以列表的形式，加载不确定行的数据；
继续使用云项目的类似fetch, promise机制封装完成数据交互；

 .then(function (response) {
        console.log(response);
      })

 .then(response => response.json())
      .then(data => {
        console.log(data)
      })


待探索疑问：

1、尝试fetch的时候，一直json化失败，后来服务器端返回改为  res.end('"login_ok"');，才搞定了，然后才能获取到data的数据。

2、目前仅仅解决了返回的对象json化的问题，但是不能设置返回的status的值，如果设置，那么，json化会失败，也就获取不到data的值。目前服务端的?耄鞘褂迷嫉膎ode.js写的，并未用到express。

3、通过函数名去调用，和通过函数返回的对象去调用，有什么区别？
